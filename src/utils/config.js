const config = {
    development: {
      apiEndpoint: 'http://localhost:3001/', // Substitua pelo seu endpoint de desenvolvimento
    },
    production: {
      apiEndpoint: 'https://api.example.com', // Substitua pelo seu endpoint de produção
    },
  };
  
  export default config;